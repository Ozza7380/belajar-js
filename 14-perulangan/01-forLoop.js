// 1. for loop 
//struktur= for (inisialisasi; kondisi; increment) { kode yang diulang}

// cetak "perulanagan ke-" sampai 5 kali
for (let i = 1; i <= 5; i++) { // for = perulangan, let i = 1 (membuat variable baru), i lebih kecil sama dengan 5, i++ = perulangan 1x (jika sudah menjalankan "perulangan ke-" samapai lima kali maka akan berhenti), i+2 perulangan 2x (jika sudah menjalankan "perulangan ke-" samapai lima kali maka akan mengulangi nya sekali lagi)
    console.log("perulangan ke-" + i) // 
}